{
  "name": "monitor-platform-server",
  "version": "0.0.1",
  "description": "Awesome project developed with TypeORM.",
  "devDependencies": {
    "ts-node": "3.3.0",
    "@types/node": "^8.0.29",
    "typescript": "3.3.3333",
    "husky": "^6.0.0",
    "prettier": "^2.2.0",
    "@commitlint/cli": "^12.1.1",
    "@commitlint/config-conventional": "^12.1.1"
  },
  "dependencies": {
    "typeorm": "0.2.32",
    "reflect-metadata": "^0.1.10",
    "mysql": "^2.14.1",
    "express": "^4.15.4",
    "body-parser": "^1.18.1"
  },
  "scripts": {
    "start": "ts-node src/index.ts",
    "restart": "./restart.sh",
    "prepare": "husky install",
    "lint": "npm run lint:js && npm run lint:style && npm run lint:prettier",
    "lint:prettier": "prettier --check 'src/**/*.{js,jsx,tsx,ts,scss,md,json}'",
    "fix:prettier": "npm run lint:prettier -- --check=false --write"
  }
}
